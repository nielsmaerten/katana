export RESTIC_REPOSITORY=s3:https://fsn1.your-objectstorage.com/katana/katana-backup
export RESTIC_PASSWORD=your_restic_password

# Optional: If using S3-compatible storage
export AWS_ACCESS_KEY_ID=your_aws_access_key_id
export AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
export AWS_DEFAULT_REGION=your_aws_region

# Optional: Call a webhook after completing a job
export POST_BACKUP_WEBHOOK="https://ntfy.sh/random-restic-backup"
export POST_CLEANUP_WEBHOOK="https://ntfy.sh/random-restic-cleanup"
